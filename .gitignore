node_modules
dist
.DS_Store
.superpowers/
.claude/
test-data/results/
!test-data/results/baseline/

# Eval reports (generated per run)
test-data/results/*/report.html

# Bench results & playwright user-data
test-data/bench-results/
bench/.user-data/

# Playwright e2e artifacts (root-level outputs from `npm run test:e2e`)
/test-results/
/playwright-report/

# Desktop (Electron) build artifacts
dist-desktop/
release/
# Embedded models — downloaded by `npm run desktop:fetch-models`, never committed
models/

# Downloaded SGJP source dump (docs/sgjp-compile.md) — the LICENSED source
# file (sgjp-*.tab.gz) never enters the repo, only the compiled artifact
# (src/verifier/morph/data/morph-pl.json) + its lock/report do.
scripts/.cache/
